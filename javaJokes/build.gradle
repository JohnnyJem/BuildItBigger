apply plugin: "java"

sourceCompatibility = 1.7

repositories {
    jcenter()
}

dependencies {
    testCompile 'junit:junit:4.11'
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}